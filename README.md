# Laravel (Docker)

## Clone sonrası (her bilgisayarda)

```bash
docker compose build
docker compose up -d
```

Container başlarken otomatik: `.env` oluşturma, `composer install`, migration. Başka işlem gerekmez.

Uygulama: http://localhost:8000
